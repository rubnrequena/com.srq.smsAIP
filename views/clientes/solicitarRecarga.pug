extends ../layoutMenu
  
block content
  .normalheader.transition.animated.fadeIn
    .hpanel
      .panel-body
        a.small-header-action(href="")
          .clip-header
            i.fa.fa-arrow-up
        #hbreadcrumb.pull-right.m-t-lg
          ol.hbreadcrumb.breadcrumb
            li
              a(href="/") Panel de Control
            li.active
              span Recargar
        h2.font-light.m-b-xs
          | Centro de Recargas
        small Solicitar recargas en linea
  .content.animate-panel
    .panel
      .panel-body
        form#recargarForm.ajax(action="/recarga/solicitud" method="post")
          .row            
            .form-group.col-lg-6
              label Recibo
              input.form-control(type="text" value="" id="" name="recibo")
            .form-group.col-lg-6
              label Monto
              input.form-control(type="text" value="" id="" name="monto")
            .form-group.col-lg-6
              label Fecha
              input.form-control(type="text" value="" id="" name="fecha")
            .form-group.col-lg-6
              label Banco
              select#paquetes.s2.form-control(name="paquete")
                option(value="banesco") Banesco
                option(value="mercantil") Mercantil
                option(value="venezuela") Venezuela
            .form-group.col-lg-12
              label Paquete
              select#paquetes.s2.form-control(name="paquete")
                option(value="pkg02") Paquete Mini
                option(value="pkg01") Paquete Inicial
          .text-center
            button.btn.btn-success(type="submit") Solicitar
            button.btn.btn-default(type="reset") Cancelar
  .panel
    .panel-body
      
block append footer
  script.
    var rch = $('#recargarForm');
    var go=false;
    rch.submit(function (e) {
      if (go==false) {
        e.preventDefault(e);
        swal({
            title: "Confirma desea recargar saldo?",
            text: "Lorem Ipsum is simply dummy text of the printing and typesetting industry.",
            showCancelButton: true,
            confirmButtonText: "Confirmar",
            cancelButtonText: "Cancelar",
            closeOnConfirm: true,
            closeOnCancel: true },
        function (isConfirm) {
          if (isConfirm) {            
            go=true;
            rch.submit();
          }
        });
      };
    });